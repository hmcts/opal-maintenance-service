meta {
  name: auth-check
  type: http
  seq: 2
}

get {
  url: {{baseURL}}/testing-support/auth/check
  body: none
  auth: bearer
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
